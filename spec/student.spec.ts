describe("Student", function() {
  beforeEach(function() {});

  it("should create a new school", function() {});

  it("should get student's profile", function() {});

  it("should add quizzes to take", function() {});

  it("should get all quizzes", function() {});

  it("should get a single quiz", function() {});

  it("should solve a single quiz", function() {});

  it("should move to unanswered next question", function() {});
});
